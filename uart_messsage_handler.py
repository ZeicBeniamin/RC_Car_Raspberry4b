class UartMessageHandler:
    def __init__(self):
        """ Initialize the variables of the class
        """
        self._rx_buffer1 = ""
        self._rx_buffer2 = ""

        # _in_use_buffer works as a "pointer" variable; it holds the
        # addres of the currently used buffer 
        self._in_use_buffer = self._rx_buffer1
        pass

    def receive_msg():
        """ Receive UART message from reading thread 

        UART message is stored in the attributes of the class. We use a
        double buffer system for storing data

        """

        if (_swap_flag == BUFFER_READY):
            if(_in_use_buffer)
        pass

    def read_msg():
        
        pass


